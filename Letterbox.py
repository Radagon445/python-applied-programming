class Letterbox:
    def __init__(self, flagup):
        self.flagup = flagup

flagup = True

    def lettersent(self):
        if flagup == True:
            print(f"{self.name} has no mail.")
        else:
            print(f"{self.name} has mail.")

